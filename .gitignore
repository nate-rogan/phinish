__pycache__/
*.py[cod]
*$py.class

.pytest_cache/
.ruff_cache/
.pixi/

*.egg-info/
.venv/
venv/

.env
.env.local

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Claude Code local settings (per-user, not committed)
.claude/settings.local.json

# Logs
*.log

# Phish.net raw API data is not stored permanently in the repo (per API ToS).
# Aggregate features (state/features/) are derivative stats and committed
# alongside the models — same legal/ethical posture as the trained model files.
data/raw/
data/processed/
